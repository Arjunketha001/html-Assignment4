Use semantic HTML elements which convey meaning and structure (like <header>, <footer>, <article>, etc.).

Ensure text contrasts sufficiently with the background.

Provide alt text for images so screen readers can describe them to visually impaired users.
